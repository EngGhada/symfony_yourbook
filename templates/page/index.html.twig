{% extends 'base.html.twig' %}

{% block title %}Hello PageController!{% endblock %}
{% block stylesheets %}
    {{ parent() }}
    <style>
        .hero-section {
            display: flex;
            height: 100vh;
            overflow: hidden;
            font-family: 'Segoe UI', sans-serif;
        }

        .hero-image {
            flex: 1;
            background: url('https://images.unsplash.com/photo-1529156069898-49953e39b3ac?auto=format&fit=crop&w=800&q=80') no-repeat center center;
            background-size: cover;
            position: relative;
        }

        .floating-book {
            position: absolute;
            bottom: 10%;
            right: 10%;
            width: 60px;
            animation: float 3s ease-in-out infinite;
        }

        @keyframes float {
            0% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
            100% { transform: translateY(0); }
        }

        .hero-content {
            flex: 1;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 2rem;
            background-color: #f8f9fa;
            text-align: center;
            animation: fadeIn 2s ease-out;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .hero-content h1 {
            font-size: 3rem;
            margin-bottom: 1rem;
        }

        .hero-content p {
            font-size: 1.2rem;
            margin-bottom: 2rem;
            max-width: 500px;
        }

        .hero-content .btn {
            background-color: #007bff;
            color: white;
            padding: 10px 20px;
            border-radius: 6px;
            font-size: 1.1rem;
            text-decoration: none;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
            transition: background 0.3s ease;
        }

        .hero-content .btn:hover {
            background-color: #0056b3;
        }

        @media (max-width: 768px) {
            .hero-section {
                flex-direction: column;
            }

            .hero-image, .hero-content {
                flex: unset;
                width: 100%;
                height: 50vh;
            }

            .floating-book {
                width: 40px;
                bottom: 5%;
                right: 5%;
            }
        }
    </style>
{% endblock %}

{% block body %}

 {# /* <style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }

    
</style>  */ #}

{#<div class="example-wrapper">

     {# <h1>Your book</h1> #}
     
    {# <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code>C:/Users/eng_a/symfony_yourbook/src/Controller/PageController.php</code></li>
        <li>Your template at <code>C:/Users/eng_a/symfony_yourbook/templates/page/index.html.twig</code></li>
    </ul> 
   
 

</div>#}

<div class="hero-section">
    <div class="hero-image">
        <div class="overlay"></div>
    </div>
    <div class="hero-content">
        <h1>Your Book</h1>
        <p>A curated space where knowledge lives. Discover your next favorite book, manage your collection, and explore stories waiting to be told.</p>
        <div class="button-group">
            <a href="{{ path('app_livre_index') }}" class="btn btn-primary">Explore the Library</a>
            <a href="{{ path('app_login') }}" class="btn btn-secondary">Admin Login</a>
        </div>
    </div>
</div>

{% endblock %}
